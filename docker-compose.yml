services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ConnectionStrings__Orders=Data Source=/app/data/orders.db
    volumes:
      - orders-data:/app/data

volumes:
  orders-data:
